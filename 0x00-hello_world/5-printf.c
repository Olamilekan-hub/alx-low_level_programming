#include <stdio.h>

/* 
 * main - Prints a line of code using printf
 *
 * return: always 0 (success)
 */
int main(void)
{
	/* print a messsage to the standard output */
	printf("with proper grammar, but the outcome is a piece of art,\n");
	return(0);
}
